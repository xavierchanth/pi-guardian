# Dashboard delivery status

MG-2 intentionally defers the following reviewed items to their owning DAG nodes without adding behavior early:

- V-6 → K7′ dashboard-tabs extraction.
- V-7 → K8 detail-height deletion.
- V-8 → K8 remaining domain bindings.
- V-9 → K8 responsive frames.
- V-11 → K8 refocus-on-reinvoke.
- V-12 → K8 allowlist purity fence.
- `x` cancel confirmation and selected-count presentation → K8/K9.
- Async hydrator consumption by Tasks/Workspaces → K7′/TA-S3. Subagents remains a synchronous, unpaged session projection.
