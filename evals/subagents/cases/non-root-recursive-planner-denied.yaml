version: 1
suite: subagents
id: non-root-recursive-planner-denied
title: "Non-root child cannot recursively create a planner workspace"
execution: specification-only
setup: { kind: subagent-harness, parentRole: root, childRole: planner, childOutcome: running }
interaction: { kind: recursive-planner-workspace, requesterRole: planner }
assertions:
  - { kind: protocol-invariant, invariant: deny-recursive-planner-workspace }
